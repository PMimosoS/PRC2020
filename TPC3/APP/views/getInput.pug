extends layout

block content
  script(src="/javascripts/jquery-3.2.1.min.js")
  script(src="/javascripts/ajax.js")
  div(class="w3-container")
    header(class="w3-container w3-center w3-teal")
      h1 SPARQL
  div(class="w3-row")
    div(class="w3-col s6 w3-padding")
        div(class="w3-container  w3-border w3-light-grey")
            header(class="w3-container")
                h4 Escolher Repositório:
                each r in repositorios.results.bindings
                  p= r.title.value
                    input.repositorio(type='radio', name="repositorio", value=r.id.value)
        div(class="w3-container  w3-border w3-light-grey")
            header(class="w3-container")
                h4 Input:
        div(class="w3-container w3-border")
            textarea(rows="10" cols="50" class="w3-input" placeholder="Input here..." name="intext" id="intext")
            input.formato(type='radio', name='formato', value='xml')
            |  XML
            input.formato(type='radio', name='formato', value='json')
            |  JSON
            div(class="w3-bar w3-pale-green")
              div(class="w3-bar-item w3-button" id="proc") Processar
              div(class="w3-bar-item w3-button" id="novo") Novo
    div(class="w3-col s6 w3-padding")
        div(class="w3-container w3-border w3-light-grey")
            header(class="w3-container")
                div(class="w3-button w3-teal w3-right" id="limpa") Limpar resultados
                h4 Resultado:
        div(class="w3-code w3-border" id="resultado")
  div(class="w3-container")
    footer(class="w3-container w3-teal")
